# Secure Hashing and Encryption

This program demonstrates SHA-256 hashing, substitution cipher encryption, and digital signatures. The program input is first encrypted and decrypted using a simple Caesar cipher, then an SHA-256 hash is generated for the input. An RSA signature is then created using the hash, which is then verified. The hashing, signing, and verifying is all done through the OpenSSL library.
